name: Flowzone

on:
  pull_request:
    types: [opened, synchronize, closed]
    branches:
      - balena

jobs:
  flowzone:
    name: Flowzone
    uses: product-os/flowzone/.github/workflows/flowzone.yml@7d7f4546307e35ff5c07f7859ae010fac8d6e873 # master
    secrets: inherit
    with:
      # Disable versioning as we don't want to version the firmware or our packaging workflows.
      # We also don't want Flowzone to finalize balena releases unless they are tagged upstream.
      disable_versioning: true
